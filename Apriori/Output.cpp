#include "Apriori.h"

void CoutOutput(Lk lk, transaction t){ // lk1이 lk-1, lk2가 lk
	cout << "Output 출력"<< endl;
	//cout << Sup_Freq->second;
	//부분 집합을 구한다.

	

	//부분집합의 나머지로 sup를 구한다. sup(s)/sup(t-s) sup(t)/sup(t-s)
}